# Facebook Auction Tracker - Server Environment Example
# --------------------------------------------------
# Copy this file to .env and fill in the required values.
# DO NOT commit real secrets.

# Core Facebook app credentials
FACEBOOK_APP_ID=1347679723547415
FACEBOOK_APP_SECRET=

# Web/app origins
# Frontend origin (new UI)
CLIENT_ORIGIN=https://facebook-group-auction-tracker-app.onrender.com
# Public base URL of this API
SERVER_BASE_URL=https://facebook-auction-api.onrender.com

# Sessions
SESSION_SECRET=replace-with-strong-secret

# OAuth scopes
# Keep as-is unless you have a specific override; these enable Page webhook flows
FACEBOOK_OAUTH_SCOPES=public_profile,pages_show_list,pages_manage_metadata,pages_read_engagement

# Webhook verify token (any random string; must match value configured in Facebook Webhooks settings)
FACEBOOK_WEBHOOK_VERIFY_TOKEN=replace-with-random-verify-token

# Optional CORS overrides (comma-separated list), only if you have additional frontends
CORS_ADDITIONAL_ORIGINS=

# Optional: only required if using extension ingest endpoint
INGEST_TOKEN=replace-with-random-long-token

# --------------------------------------------------
# Facebook App configuration checklist (complete in Facebook for Developers)
#
# App basics
# - App ID:          1347679723547415 (already set above)
# - App secret:      Set in this .env (do not commit)
# - Display name:    Auction Tracker (or your preferred name)
# - Namespace:       Optional
# - App domains:
#     - facebook-group-auction-tracker-app.onrender.com
#     - facebook-auction-api.onrender.com
# - Contact email:   Your contact email
# - Privacy policy URL:      https://<your-domain>/privacy
# - Terms of Service URL:    https://<your-domain>/terms
# - User data deletion URL:  https://<your-domain>/data-deletion (or instructions page)
# - App icon (1024 x 1024):  Upload a 1024x1024 PNG
# - Category:                Choose an appropriate category for your app
# - (Optional) DPO contact info (GDPR): Name, Email, Address
#
# Facebook Login
# - Valid OAuth Redirect URIs:
#     - https://facebook-auction-api.onrender.com/auth/facebook/callback
#
# Webhooks product
# - Add product: Webhooks
# - Configure callback URL:
#     - https://facebook-auction-api.onrender.com/webhook/facebook
# - Verify token: use FACEBOOK_WEBHOOK_VERIFY_TOKEN value above
# - Subscribe for object: page, field: feed (used for realtime comments on Page posts)
#
# Permissions (App Review → Permissions and features)
# - Add these to your app (appear under "Added items" for Development testing with app roles):
#     - pages_show_list
#     - pages_manage_metadata
#     - pages_read_engagement
# - For production (non-role users), submit these for review with required details.
#
# Roles (Development testing)
# - Add your Facebook account as Admin/Developer of the app
# - Log in via the app with the Page-admin account that is also an app role
#
# After configuration
# - In the web app (Settings), click "Reload pages" and then subscribe your Page (bell icon)
# - Comments on Page posts will stream to your clients via Webhooks -> WS/SSE
